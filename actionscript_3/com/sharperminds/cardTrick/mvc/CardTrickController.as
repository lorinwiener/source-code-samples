/* Copyright (c) 2011  Lorin Wiener  <lorin@lorinwiener.com> All rights reserved. */package  com.sharperminds.cardTrick.mvc {		//--------------------------------------	// IMPORTS	//--------------------------------------	import flash.display.DisplayObject;	import flash.events.*	import flash.events.MouseEvent;		//--------------------------------------	// CLASS	//--------------------------------------	/**	 * Concrete MVC controller class to handle communication from view to model	*/	public class CardTrickController extends Controller	{				//--------------------------------------		// CONSTRUCTOR		//--------------------------------------		/**	 	 * Contructor to establish the model for the MVC architecture		*/		public function CardTrickController (argModel : Model)		{			// SUPER			super (argModel);		}				//--------------------------------------		// METHODS		//--------------------------------------						// PUBLIC		public function initCards (event : Event = null)		{			model.initCards ();		}				public function flipCard (argCardPosition : uint, argCardState : String)		{			model.flipCard (argCardPosition, argCardState);		}				public function getCardState (argCardPosition : uint) : String		{			return model.getCardState (argCardPosition);		}				public function getCardValue (argCardPosition : uint) : String		{			return model.getCardValue (argCardPosition);		}				public function getCardSuit (argCardPosition : uint) : String		{			return model.getCardSuit (argCardPosition);		}	}	}